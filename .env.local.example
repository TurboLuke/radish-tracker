# For local development only — in Vercel these are injected automatically
# when you add the Upstash Redis integration from the Storage tab.
KV_REST_API_URL=https://your-db.upstash.io
KV_REST_API_TOKEN=your-token-here
